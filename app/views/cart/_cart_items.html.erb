<% @order_items.each do |item| %>
<h3><%= item.product_listing.name %></h3>
<h3><%= item.product_listing.price %></h3>
<%= form_with model: item do |f| %>
    <%= f.hidden_field :product_listing_id, value: item.product_listing.id %>
    <%= f.number_field :quantity, value: item.quantity.to_i %>
    <%= f.submit "Update Cart" %>
    <%= link_to "Remove Item", item, method: :delete, remote: true %>
<% end %>
<h2>Subtotal: <%= item.total %></h2>
<% end %>

    <h3>Your current order has a total of: $<%= current_order.subtotal %></h3>

 <h3>You haven't added anything to your cart yet!</h3>
